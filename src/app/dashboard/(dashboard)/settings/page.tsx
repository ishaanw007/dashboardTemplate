import React from 'react';
import { Settings } from '@/components/Settings/Settings';
function page() {
    return (
        <div>
            <Settings></Settings>
        </div>
    );
}

export default page;